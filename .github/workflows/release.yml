name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:

  # ─────────────────────────────────────────────
  # 1. LINUX — build .deb
  # ─────────────────────────────────────────────
  build-linux:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1 libglib2.0-0 libdbus-1-3 \
            libxcb-xinerama0 libxcb-cursor0 \
            dpkg-dev fakeroot

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install ".[all]"
          pip install pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller \
            --name noteration \
            --icon noteration/assets/images/icon.png \
            --collect-all PySide6 \
            --collect-all shiboken6 \
            --collect-all papis \
            --collect-all pymupdf \
            --collect-all citeproc \
            --collect-all citeproc_styles \
            --hidden-import PySide6.QtSvg \
            --hidden-import PySide6.QtPdf \
            --hidden-import PySide6.QtPdfWidgets \
            --add-data "noteration/assets:noteration/assets" \
            --add-data "noteration/docs:noteration/docs" \
            noteration/app.py

      - name: Package as .deb
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          mkdir -p pkg/DEBIAN
          mkdir -p pkg/opt/noteration
          mkdir -p pkg/usr/local/bin
          mkdir -p pkg/usr/share/applications
          
          # Copy the entire distribution directory
          cp -r dist/noteration/* pkg/opt/noteration/
          
          # Create symbolic link for the binary
          ln -s /opt/noteration/noteration pkg/usr/local/bin/noteration

          for size in 16 32 48 128 256 512; do
            ICON_PATH="pkg/usr/share/icons/hicolor/${size}x${size}/apps"
            mkdir -p "$ICON_PATH"
            cp "noteration/assets/images/icon_${size}.png" "$ICON_PATH/noteration.png"
          done

          cat > pkg/DEBIAN/control << EOF
          Package: noteration
          Version: ${VERSION}
          Architecture: amd64
          Maintainer: lilamr <https://github.com/lilamr>
          Depends: libgl1, libglib2.0-0, libdbus-1-3, libxcb-icccm4, libxcb-image0, libxcb-keysyms1, libxcb-randr0, libxcb-render-util0, libxcb-xinerama0, libxcb-xinput0, libxcb-xfixes0, libxcb-shape0, libxcb-cursor0, libxkbcommon-x11-0, libfontconfig1, libnss3, libasound2, libgbm1, libxshmfence1
          Description: Research note-taking app
           Markdown editor, PDF viewer, Git sync, Papis integration.
          EOF

          cat > pkg/usr/share/applications/noteration.desktop << EOF
          [Desktop Entry]
          Name=Noteration
          Comment=Research note-taking app
          Exec=/usr/local/bin/noteration
          Icon=noteration
          Terminal=false
          Type=Application
          Categories=Office;Education;Science;
          EOF

          fakeroot dpkg-deb --build pkg \
            noteration_${VERSION}_amd64.deb
          cp noteration_${VERSION}_amd64.deb noteration-latest-linux-amd64.deb

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-deb
          path: "*.deb"

  # ─────────────────────────────────────────────
  # 2. WINDOWS — build .exe installer
  # ─────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install ".[all]"
          pip install pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller `
            --onefile `
            --windowed `
            --name Noteration `
            --icon noteration/assets/images/icon.ico `
            --collect-all PySide6 `
            --collect-all shiboken6 `
            --collect-all papis `
            --collect-all pymupdf `
            --collect-all citeproc `
            --collect-all citeproc_styles `
            --hidden-import PySide6.QtSvg `
            --hidden-import PySide6.QtPdf `
            --hidden-import PySide6.QtPdfWidgets `
            --add-data "noteration/assets;noteration/assets" `
            --add-data "noteration/docs;noteration/docs" `
            noteration/app.py

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Create Inno Setup script
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          @"
          [Setup]
          AppName=Noteration
          AppVersion=$version
          AppPublisher=lilamr
          AppPublisherURL=https://github.com/lilamr/noteration
          DefaultDirName={autopf}\Noteration
          DefaultGroupName=Noteration
          OutputDir=installer
          OutputBaseFilename=Noteration-Setup-$version
          SetupIconFile=noteration\assets\images\icon.ico
          Compression=lzma
          SolidCompression=yes
          WizardStyle=modern
          UninstallDisplayIcon={app}\Noteration.exe

          [Languages]
          Name: "english"; MessagesFile: "compiler:Default.isl"

          [Tasks]
          Name: "desktopicon"; Description: "Create a desktop shortcut"; GroupDescription: "Additional icons:"

          [Files]
          Source: "dist\Noteration.exe"; DestDir: "{app}"; Flags: ignoreversion

          [Icons]
          Name: "{group}\Noteration"; Filename: "{app}\Noteration.exe"
          Name: "{group}\Uninstall Noteration"; Filename: "{uninstallexe}"
          Name: "{commondesktop}\Noteration"; Filename: "{app}\Noteration.exe"; Tasks: desktopicon

          [Run]
          Filename: "{app}\Noteration.exe"; Description: "Launch Noteration"; Flags: nowait postinstall skipifsilent
          "@ | Out-File -FilePath noteration.iss -Encoding UTF8

      - name: Compile installer
        shell: pwsh        
        run: |
          if (!(Test-Path installer)) { mkdir installer }
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" noteration.iss
          $version = "${{ github.ref_name }}".TrimStart('v')
          cp installer/Noteration-Setup-$version.exe installer/noteration-latest-windows-x64.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: installer/*.exe

  # ─────────────────────────────────────────────
  # 3. macOS — build .dmg
  # ─────────────────────────────────────────────
  build-macos:
    runs-on: macos-latest
    env:
      MACOSX_DEPLOYMENT_TARGET: "12.0"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: brew install create-dmg

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install ".[all]"
          pip install pyinstaller

      - name: Build .app bundle with PyInstaller
        run: |
          pyinstaller \
            --windowed \
            --name Noteration \
            --icon noteration/assets/images/icon.icns \
            --collect-all PySide6 \
            --collect-all shiboken6 \
            --collect-all papis \
            --collect-all pymupdf \
            --collect-all citeproc \
            --collect-all citeproc_styles \
            --hidden-import PySide6.QtSvg \
            --hidden-import PySide6.QtPdf \
            --hidden-import PySide6.QtPdfWidgets \
            --add-data "noteration/assets:noteration/assets" \
            --add-data "noteration/docs:noteration/docs" \
            --osx-bundle-identifier com.lilamr.noteration \
            noteration/app.py

      - name: Create .dmg
        run: |
          VERSION=${GITHUB_REF_NAME#v}

          create-dmg \
            --volname "Noteration $VERSION" \
            --volicon "noteration/assets/images/icon.icns" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Noteration.app" 150 200 \
            --hide-extension "Noteration.app" \
            --app-drop-link 450 200 \
            "Noteration-${VERSION}.dmg" \
            "dist/Noteration.app"

          cp Noteration-${VERSION}.dmg noteration-latest-macos.dmg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: "*.dmg"

  # ─────────────────────────────────────────────
  # 4. PYTHON WHEEL — untuk pip install
  # ─────────────────────────────────────────────
  build-wheel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build wheel and sdist
        run: |
          pip install build
          python -m build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-wheel
          path: dist/*

  # ─────────────────────────────────────────────
  # 5. RELEASE — kumpulkan semua & publish
  # ─────────────────────────────────────────────
  release:
    needs: [build-linux, build-windows, build-macos, build-wheel]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Noteration ${{ github.ref_name }}"
          body: |
            ## Installation

            | Platform | File | How to install |
            |---|---|---|
            | Linux (Ubuntu/Debian) | `noteration_*_amd64.deb` | `sudo dpkg -i noteration_*_amd64.deb` |
            | Windows | `Noteration-Setup-*.exe` | Run the installer |
            | macOS | `Noteration-*.dmg` | Open and drag to Applications |
            | Any (pip) | `noteration-*.whl` | `pip install noteration[all]` |

            See [CHANGELOG.md](https://github.com/lilamr/noteration/blob/main/CHANGELOG.md) for what's new.
          files: release-assets/*
          generate_release_notes: false
          draft: false
          prerelease: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
